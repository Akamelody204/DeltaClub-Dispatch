# ⚠️ 接口表已迁移

**唯一契约真源**已迁至：

- **`plan/product/dev/接口表-v0.md`**（当前 **v0.9+**）

## 产品约定（P1-1 起持续有效）

实现时若 **Path / 字段 / 业务码** 需微调，须 **三处同改** 保持一致：

1. **`server/`** 实现代码  
2. **`plan/product/dev/接口表-v0.md`**（修订记录升版）  
3. **`docs/API_CONTRACT.md`**（镜像与版本号）

> 历史说明：P1-1 当时 **`server/`** 仅 **`GET /health`**、上表为初稿；**当前** 已实现认证、订单、管理端等，**上述同步规则不变**，仍适用于后续迭代与 Path 调整。

请勿在本文件继续增改业务接口。

---

*本文件保留为历史路径兼容与书签跳转。*
