# M1-5 · 合法域名与 staging 联调

> **对应任务**：小程序负责人工作计划 · Phase 1 · **M1-5**。  
> **目的**：微信公众平台配置 **request 合法域名**；小程序 **staging 构建** 的 **`VITE_APP_API_BASE_URL`** 与 **HTTPS staging API 根**一致；**真机**可访问 staging（登录或任意需鉴权接口成功）。  
> **依据**：`../../backend/B1-5-staging部署与合法域名.md`（**主协同**）、`../../backend/B0-2-运行环境与域名.md` §2；`M0-3-环境切换与baseURL.md`；`../../Phase0-收口与Phase1开启.md` §4（B1-5 与 M1-5 并行）。

---

## 1. 与设计 / 领头人文档的关系（阅读结论）

| 文档 | 与 M1-5 关系 |
|------|----------------|
| **`../../design/与其他端的对接说明-非design目录曾由会话改动清单.md`** | 记载 Phase0 收口时曾改 **`小程序负责人工作计划`**、**`Phase0-项目全貌…`** 等；**不替代**域名部署操作；设计真源仍在 **`plan/design/dev/`**。 |
| **`../../design/dev/D0-1-主路径页面清单与优先级对齐.md` §4** | 路由映射以 **`miniapp/src/pages.json`** 为准；订单详情、陪玩大厅已在工程注册（与早期「未注册」表述若有出入，以代码与 **M0-2/M1-3/M1-4** 为准）。 |
| **`../../backend/B1-5-staging部署与合法域名.md`** | **staging 部署、HTTPS、微信公众平台 request 域名** 的**执行清单真源**；本文侧重 **小程序侧**勾选与验证。 |

---

## 2. 小程序侧必须一致的三项

| 项 | 要求 |
|----|------|
| **API 根 URL** | **仅根**，**无** `/api/v1` 后缀，**无**尾斜杠；与 `miniapp/.env.staging` 中 **`VITE_APP_API_BASE_URL`** 一致（例：`https://api-staging.example.com`）。 |
| **构建命令** | `npm run build:mp-weixin:staging`（读取 `.env.staging`；本机真值可用 **`.env.staging.local`** 覆盖，已 gitignore）。 |
| **微信后台** | **开发 → 开发管理 → 开发设置 → 服务器域名 → request 合法域名**：填写 **与上表同一根**（协议 + 域名，路径不写）。 |

**注意**：`VITE_*` 会进包体，只放**可公开的 API 根**，不放密钥。

---

## 3. 微信公众平台操作步骤（摘要，细节以 B1-5 §4 为准）

1. 确认 **staging 已 HTTPS** 且 `GET https://<根>/health` 返回 200（后端 **B1-5**、**`server/docs/DEPLOY-STAGING.md`**）。  
2. 登录 [微信公众平台](https://mp.weixin.qq.com/)（**与小程序 AppID 一致**）。  
3. **开发 → 开发管理 → 开发设置 → 服务器域名**：配置 **request 合法域名**。  
4. 若需 **域名校验文件**，按平台提示放到根域可访问路径（见 B1-5 §4.3）。  
5. 一期仅 REST：**uploadFile / downloadFile / socket** 等按需再开；通常 **先配 request 即可**。

---

## 4. 开发者工具 vs 真机

| 场景 | 说明 |
|------|------|
| **开发** | 可勾选「不校验合法域名、web-view、TLS 版本」连 **本地 HTTP**；**不能**代替 M1-5 验收。 |
| **M1-5 验收** | **真机预览** 或 **关闭不校验** 后的开发者工具，使用 **staging 构建包**，请求须走 **已配置的 HTTPS 合法域名**。 |

---

## 5. 联调验证（建议顺序）

1. 本地改 **`miniapp/.env.staging.local`**（新建）：`VITE_APP_API_BASE_URL=https://<真实 staging 根>`。  
2. `npm run build:mp-weixin:staging`，导入 `dist/build/mp-weixin`。  
3. 真机：打开小程序 → 登录（或 **我的** / 首页触发 `GET /me`）→ **订单列表** 等带 token 请求。  
4. 期望：无「不在以下 request 合法域名列表中」；接口返回正常或业务错误（非域名失败）。

---

## 6. 产出物（完成标志）

| 产出 | 说明 |
|------|------|
| **配置完成** | 微信后台 **request 合法域名** 已保存（截图或运营书面确认，**勿**含密钥）。 |
| **工程一致** | 仓库内 **`.env.staging`** 仍为占位亦可；**团队共用真值** 建议 **`.env.staging.local`** 或个人不提交分支策略（与 **M0-3** 一致）。 |
| **接口表（可选）** | staging 真值回填 **`../../product/dev/接口表-v0.md` §1.1**（与 **B0-2 §5** 说明一致）。 |

**完成标志（M1-5）**

- [ ] 上表 **真机访问 staging API 成功** 已达成，执行人填日期。  
- [ ] 若公网 staging **尚未就绪**：保持未勾选，但 **B1-5 §5** 与本文已作为唯一操作依据；就绪后补勾选。

---

## 7. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-04 | 初稿：对齐 B1-5、Phase0 收口、设计改动清单阅读结论 |
