# P1-2 · 小程序文案与业务码对齐

> **真源**：`plan/product/dev/P1-2-文案包-v0.md`（v0.1）、`plan/product/dev/接口表-v0.md` §7。  
> **代码落点**：`miniapp/src/constants/copy.ts`（页面文案常量 + `mapBusinessCodeToMessage`）；`miniapp/src/utils/request.ts`（失败时优先按 `code` 映射）；`miniapp/src/utils/unauthorized.ts`（40101 / HTTP 401 与 §3「登录已过期…」一致）。

## P1-3 · 订单状态短标签（与 P1-2 §6 关系）

- **真源**：`plan/product/dev/P1-3-订单状态与用户可见文案映射表-v0.md` §2（**勿**在 `copy.ts` 再维护一套状态枚举文案）。  
- **代码**：`miniapp/src/utils/order-status-text.ts` · `formatOrderStatusText(status, designatedCompanionUserId, statusTextFallback)`。  
- **使用页**：订单列表、订单详情、陪玩大厅列表；`PENDING_GRAB` + 点名 →「待 TA 接单」，否则「待接单」。未知 `status` 时回退接口 `statusText`。

## P1-4 · 入口与 Tab

- **真源**：`plan/product/dev/P1-4-玩家与陪玩入口及Tab分流规则-v0.md`。  
- **工程核对**：底部四 Tab 顺序为首页 → 下单 → 订单 → 我的；陪玩大厅为 `navigateTo('/pages/companion/pending')`（与 `pages.json` 路径一致）。与 **`M0-4-选型接口与入口规则对齐.md` §5** 一致。

## 完成标志（产品 §9 小程序侧）

- [ ] 负责人在站会或迭代内确认：**可按 P1-2 §4～§5 落稿**，且**无新增未命名主按钮**（与 `D1-3` 对照）。

## 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-04 | 初稿：与产品 P1-2 广播对齐，索引代码位置 |
| v0.2 | 2026-04-06 | 增补 P1-3 / P1-4 真源与代码索引（Phase 1 收口对齐） |
